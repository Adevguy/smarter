<!DOCTYPE html>
<html lang="<%= lang === 'ar' ? 'ar' : 'en' %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
  <link href="https://fonts.googleapis.com/css2?family=Alexandria:wght@100..900&display=swap" rel="stylesheet">
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/categories.css" />
  </head>
  <body dir="<%= lang === 'ar' ? 'rtl' : 'ltr' %>" style="overflow-x: hidden">
    <%-include("navbar")%>
    <div class="container">
    <div class="mt-5 row align-items-center justify-content-center">
      <!-- Text Column -->
      <div class="col-md-6 col-12 text-md-end text-center">
        <h2 class="display-2 text-white fw-bold"><%= t.ourProducts %></h2>
        <p class="text-white fs-5">
          <%= t.categoriesSubtitle %>
        </p>
        <a href="/contact" dir="<%=lang === "ar" ? "rtl" : "rtl"%>"
          class=" fs-5 w-50 text-white header-btn mx-auto mx-md-0 d-block d-md-inline"
         style="padding: 20px; text-decoration: none;">
          <%= t.orderNow %> <img src="/arrow.svg" alt="arrow svg" />
        </a>
      </div>

      <!-- Image Column -->
      <div class="col-md-6 col-12 text-center">
        <img src="/about1.png" alt="header image" class="img-fluid" />
      </div>
    </div>
    <hr
      class="mt-5 w-md-50"
      style="background-color: #ef6341; opacity: 1; height: 5px"
    />
    <div class="categories">
      <div class="row g-4">
        <%for(let category of categories){%>
        <div class="col-md-6 my-3">
          <div class="card p-3" style="background-color: #c9735e">
            <img src="/cups.png" alt="cup product" class="img-fluid" />
            <h3 class="text-white fw-bold"><%=category.name%></h3>
            <p class="text-white">
              <%=category.description%>
            </p>
            <a
              href="/products/<%=category.name%>"
              class="text-white mt-3 text-decoration-none explore-btn <%= lang === 'en' ? 'center-en' : '' %>"
              ><%= t.exploreCategory %> <img src="/arrow.svg" alt="arrow"
                class="<%= lang === 'en' ? 'arrow-rotate-en' : '' %>"
              /></a>
          </div>
        </div>

        <%}%>
      </div>
    </div>
    </div>
    <%-include("footer")%>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
